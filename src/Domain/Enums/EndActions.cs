namespace EDO_FOMS.Domain.Enums
{
    public enum EndActions : int
    {
        Undefined = 0,   // Роль не определена
        SignedByAll = 1, // Подписано всеми
        ToArchive = 2    // В архив
    }
}
