namespace EDO_FOMS.Domain.Enums
{
    public enum ActTypes : int
    {
        Undefined = 0,

        Signing = 1,   // Подписание
        Agreement = 2, // Согласование
        Review = 3     // Рецензирование
    }
}
